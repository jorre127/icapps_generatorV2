name: icapps_generator_example
description: A project used to demo the icapps generator
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  kiwi: ^0.2.0
  provider: ^3.1.0

dev_dependencies:
  build_runner: ^1.7.0
  flutter_test:
    sdk: flutter
  icapps_generator:
    path: ../
  kiwi_generator: 0.4.0